const authService = {
  login: async () => {
    throw new Error("Authentication will be connected to Supabase.")
  },

  register: async () => {
    throw new Error("Authentication will be connected to Supabase.")
  },
}

export default authService